spring:
  datasource:
    url: jdbc:mysql://mysql:3306/mall_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai
    username: root
    password: ${MYSQL_ROOT_PASSWORD:root123}
  data:
    redis:
      host: redis
      port: 6379

feign:
  base-service:
    url: http://base-service:9001
