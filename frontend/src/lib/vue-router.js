const router = window.VueRouter;

export const createRouter = router.createRouter;
export const createWebHashHistory = router.createWebHashHistory;
export const useRouter = router.useRouter;
export const useRoute = router.useRoute;
